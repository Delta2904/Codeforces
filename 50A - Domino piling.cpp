#include <bits/stdc++.h>
using namespace std;

void solve(int n, int m) {
    int max = n * m;

    cout << max / 2 << endl;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int n, m;
    cin >> n >> m;

    solve(n, m);
    
    return 0;
}